# !ln -s %:p ~/.config/lazygit/config.yml
git:
  paging:
    colorArg: always
    pager: diff-so-fancy
    # pager: delta --dark --paging=never --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"
